# API Configuration
api.base.uri=https://restful-booker.herokuapp.com
api.auth.endpoint=/auth
api.booking.endpoint=/booking

# Authentication
api.username=admin
api.password=password123

# Timeouts (as strings)
api.connection.timeout=10000
api.read.timeout=30000
api.response.timeout=5

# Retry Configuration (as strings)
test.retry.count=2
test.retry.enabled=true

# Test Data
test.data.path=testdata/

# Logging (as strings)
test.debug.mode=false
api.log.requests=true
api.log.responses=true